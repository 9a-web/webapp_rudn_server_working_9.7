# Уведомления о присоединении к комнате

## Описание изменений

Реализована функция отправки уведомлений всем участникам комнаты при вступлении нового участника через реферальную ссылку.

## Изменения в коде

### 1. Backend - telegram_bot.py

#### Новые функции:

**`join_user_to_room(telegram_id, username, first_name, invite_token, referrer_id)`**
- Добавляет пользователя в комнату по токену приглашения
- Автоматически добавляет пользователя во все групповые задачи комнаты
- Возвращает информацию о комнате и участниках для отправки уведомлений
- Параметры:
  - `telegram_id`: Telegram ID нового участника
  - `username`: имя пользователя (@username)
  - `first_name`: имя пользователя
  - `invite_token`: токен приглашения комнаты
  - `referrer_id`: ID пользователя, создавшего приглашение

**`send_room_join_notifications(bot, room_data, new_user_name, new_user_id)`**
- Отправляет уведомления всем участникам комнаты о новом участнике
- Отправляет приветственное уведомление новому участнику
- Параметры:
  - `bot`: объект Telegram Bot
  - `room_data`: данные о комнате (результат `join_user_to_room`)
  - `new_user_name`: имя нового участника
  - `new_user_id`: Telegram ID нового участника

#### Обновленная функция `start_command()`:

**Добавлена обработка приглашений в комнату:**
- Распознает параметр формата `room_{invite_token}_ref_{referrer_id}`
- Извлекает `invite_token` и `referrer_id`
- Вызывает `join_user_to_room()` для добавления пользователя
- Отправляет уведомления через `send_room_join_notifications()`

**Обновлены приветственные сообщения:**
- Для нового пользователя, присоединившегося к комнате
- Для существующего пользователя, присоединившегося к комнате
- Для существующего пользователя, который уже в комнате

### 2. Backend - server.py

#### Новая функция:

**`send_room_join_notifications_api(room_doc, new_user_name, new_user_id)`**
- Отправляет уведомления через Telegram Bot API
- Используется при присоединении к комнате через Web App API
- Параметры:
  - `room_doc`: документ комнаты из MongoDB
  - `new_user_name`: имя нового участника
  - `new_user_id`: Telegram ID нового участника

#### Обновленный эндпоинт:

**`POST /api/rooms/join/{invite_token}`**
- Добавлен вызов `send_room_join_notifications_api()` после успешного добавления участника
- Отправляет уведомления всем участникам комнаты

## Форматы уведомлений

### Уведомление новому участнику:

```
🎉 Добро пожаловать в комнату!

📋 Комната: [Название комнаты]
👥 Участников: [Количество]

✅ Вы успешно присоединились к командной комнате для совместного выполнения задач!

Откройте приложение, чтобы увидеть задачи комнаты 👇
```

### Уведомление существующим участникам:

```
👋 Новый участник в комнате!

📋 Комната: [Название комнаты]
✨ К команде присоединился: [Имя нового участника]
👥 Всего участников: [Количество]

Продолжайте выполнять задачи вместе! 💪
```

## Сценарии использования

### Сценарий 1: Новый пользователь переходит по реферальной ссылке

1. Пользователь A создает комнату "Подготовка к экзамену"
2. Пользователь A генерирует реферальную ссылку: `https://t.me/rudn_pro_bot?start=room_abc123_ref_123456`
3. Пользователь B (новый) переходит по ссылке
4. Бот обрабатывает команду `/start room_abc123_ref_123456`
5. Система:
   - Создает нового пользователя B
   - Добавляет пользователя B в комнату
   - Добавляет пользователя B во все задачи комнаты
   - Отправляет приветственное уведомление пользователю B
   - Отправляет уведомление о новом участнике пользователю A

### Сценарий 2: Существующий пользователь переходит по реферальной ссылке

1. Пользователь C (уже зарегистрирован) переходит по ссылке `https://t.me/rudn_pro_bot?start=room_abc123_ref_123456`
2. Бот обрабатывает команду `/start room_abc123_ref_123456`
3. Система:
   - Добавляет пользователя C в комнату
   - Добавляет пользователя C во все задачи комнаты
   - Отправляет приветственное уведомление пользователю C
   - Отправляет уведомления о новом участнике всем существующим участникам (A, B)

### Сценарий 3: Присоединение через Web App API

1. Пользователь D открывает Web App и вводит код приглашения
2. Frontend отправляет запрос `POST /api/rooms/join/abc123`
3. Backend:
   - Добавляет пользователя D в комнату
   - Добавляет пользователя D во все задачи комнаты
   - Отправляет уведомления через `send_room_join_notifications_api()`

## Технические детали

### База данных

**Коллекция `rooms`:**
- Обновляется при добавлении нового участника
- Поле `participants` содержит массив участников
- Поле `updated_at` обновляется при изменениях

**Коллекция `group_tasks`:**
- Автоматически добавляет нового участника во все задачи комнаты
- Каждая задача имеет массив `participants`

### Логирование

Все операции логируются:
- `✅` - успешные операции
- `⚠️` - предупреждения (например, не удалось отправить уведомление)
- `❌` - ошибки
- `🏠` - операции с комнатами
- `🔗` - реферальные ссылки

### Обработка ошибок

- Если токен комнаты не найден, возвращается `None` (telegram_bot) или `404` (API)
- Если пользователь уже участник, возвращается информация о комнате без дублирования
- Если не удалось отправить уведомление участнику, логируется предупреждение, но процесс продолжается
- Если `TELEGRAM_BOT_TOKEN` не настроен, уведомления не отправляются (логируется предупреждение)

## Тестирование

### Тест 1: Создание и присоединение к комнате

```bash
# 1. Создать комнату
curl -X POST http://localhost:8001/api/rooms \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тестовая комната",
    "description": "Для тестирования уведомлений",
    "telegram_id": 123456789,
    "color": "blue"
  }'

# 2. Сгенерировать ссылку-приглашение
curl -X POST http://localhost:8001/api/rooms/{room_id}/invite-link \
  -H "Content-Type: application/json" \
  -d '{"telegram_id": 123456789}'

# 3. Присоединиться к комнате (как новый пользователь)
curl -X POST http://localhost:8001/api/rooms/join/{invite_token} \
  -H "Content-Type: application/json" \
  -d '{
    "invite_token": "abc123",
    "telegram_id": 987654321,
    "username": "test_user",
    "first_name": "Test User",
    "referral_code": "123456789"
  }'

# Проверить уведомления в Telegram
```

### Тест 2: Присоединение через Telegram Bot

1. Создать реферальную ссылку в Web App
2. Открыть ссылку в Telegram (другой аккаунт)
3. Нажать "Start"
4. Проверить уведомления:
   - Новый участник получил приветствие
   - Все участники получили уведомление о новом участнике

## Зависимости

- `python-telegram-bot>=20.0` (для Telegram Bot API)
- `motor` (для MongoDB async operations)
- `fastapi` (для REST API)
- `pymongo` (для MongoDB)

## Переменные окружения

```bash
TELEGRAM_BOT_TOKEN=your_bot_token_here
MONGO_URL=mongodb://localhost:27017
DB_NAME=test_database
```

## Статус

✅ **Реализовано:**
- Обработка реферальных ссылок формата `room_{token}_ref_{user_id}`
- Автоматическое добавление в комнату и задачи
- Отправка уведомлений новому участнику
- Отправка уведомлений всем существующим участникам
- Поддержка присоединения через Telegram Bot и Web App API

⏳ **Возможные улучшения:**
- Настройка типов уведомлений (включить/выключить)
- Групповые уведомления вместо индивидуальных
- История уведомлений
- Уведомления о других событиях (выполнение задач, комментарии и т.д.)
