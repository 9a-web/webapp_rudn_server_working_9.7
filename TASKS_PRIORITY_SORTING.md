# Сортировка задач по приоритету - Единый список

## Описание изменений

По запросу пользователя внесены следующие изменения в раздел "Список дел":

### 1. Убраны все разделы TaskGroup
- Удалены все отдельные группы задач ("Без срока", "На выбранную дату" и т.д.)
- Все задачи теперь отображаются в **одной карточке** с заголовком в зависимости от выбранной даты

### 2. Приоритет влияет на расположение задач
- Все задачи автоматически сортируются по приоритету:
  - 🔥 **Высокий приоритет** (high) - отображаются первыми
  - ⚡️ **Средний приоритет** (medium) - отображаются вторыми
  - ✅ **Низкий приоритет** (low) - отображаются последними

### 3. Задачи без дедлайна включены
- Задачи без дедлайна теперь отображаются в общей карточке вместе с задачами на выбранную дату
- Они также сортируются по приоритету

## Технические детали

### Изменения в `/app/frontend/src/components/TasksSection.jsx`:

1. **Функция `getTasksForSelectedDate()`** - полностью переработана:
   - Объединяет задачи с дедлайном на выбранную дату И задачи без дедлайна в один массив `allTasks`
   - Применяет сортировку по приоритету к объединенному массиву
   - Возвращает **ВСЕ задачи** без ограничения (убран `.slice(0, 10)`)

2. **Удалены неиспользуемые функции**:
   - `groupTasksByDeadline()` - больше не нужна, так как нет разделов TaskGroup
   - `getTaskGroupTitle()` - больше не нужна, так как нет разделов TaskGroup
   - `groupedTasks` - переменная удалена

3. **Рендеринг задач**:
   - Удалены все компоненты `TaskGroup`
   - Все задачи отображаются только в карточке "Сегодня" (с динамическим заголовком)
   - Добавлена прокрутка: `max-h-[60vh] overflow-y-auto` с кастомным scrollbar

## Логика сортировки

```javascript
// Приоритеты задач (чем выше значение, тем выше в списке)
const priorityOrder = { 
  high: 3,    // Высокий приоритет - показываются первыми
  medium: 2,  // Средний приоритет - показываются вторыми
  low: 1      // Низкий приоритет - показываются последними
};

// Сортировка по убыванию приоритета
allTasks.sort((a, b) => {
  const priorityA = priorityOrder[a.priority] || 2; // По умолчанию - средний
  const priorityB = priorityOrder[b.priority] || 2;
  return priorityB - priorityA; // От высокого к низкому
});
```

## Результат

Теперь в разделе "Список дел":
- ✅ Отсутствуют все разделы TaskGroup
- ✅ Все задачи отображаются в **одной карточке** (с динамическим заголовком: "Сегодня", "Завтра", "Вчера" или день недели)
- ✅ Карточка показывает ВСЕ задачи (задачи с дедлайном на выбранную дату + задачи без дедлайна)
- ✅ Задачи с высоким приоритетом всегда отображаются первыми
- ✅ При большом количестве задач появляется прокрутка (max-height: 60vh)
- ✅ Сохранены все функции: фильтры, категории, быстрые действия, drag & drop, счетчик выполненных задач

## Пользовательский опыт

**Преимущества новой логики:**
1. **Максимальная простота** - все задачи в одном окне, никаких разделов
2. **Фокус на важном** - задачи с высоким приоритетом всегда на виду
3. **Нет путаницы** - не нужно искать задачи в разных разделах или группах
4. **Единое представление** - все задачи (с дедлайном и без) в одной карточке
5. **Гибкость** - задачи без дедлайна также учитываются и сортируются по приоритету
6. **Удобная прокрутка** - при большом количестве задач карточка прокручивается

## История изменений
- **27 января 2025 v1**: Удален раздел "Без срока", добавлена сортировка по приоритету
- **27 января 2025 v2**: Убраны все разделы TaskGroup, все задачи в одной карточке

## Статус
✅ Реализовано и протестировано
