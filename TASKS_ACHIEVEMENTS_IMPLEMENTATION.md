# 🏆 Реализация достижений для раздела "Список дел"

## 📋 Добавленные достижения (8 штук)

### 1. 📝 Первая задача (5 очков)
- **ID:** `first_task`
- **Условие:** Создал первую задачу
- **Триггер:** При создании первой задачи через POST /api/tasks

### 2. ✅ Продуктивный день (15 очков)
- **ID:** `productive_day`
- **Условие:** Выполнил 5 задач за день
- **Триггер:** При выполнении задачи, когда tasks_completed_today >= 5

### 3. 🌅 Рано встаешь (15 очков)
- **ID:** `early_riser_tasks`
- **Условие:** Выполнил 10 задач до 9:00 утра
- **Триггер:** При выполнении задачи до 9:00, когда tasks_completed_early >= 10

### 4. 🎯 Специалист по задачам (20 очков)
- **ID:** `task_specialist`
- **Условие:** Создал 50 задач
- **Триггер:** При создании задачи, когда tasks_created_total >= 50

### 5. ⚡ Молния (25 очков)
- **ID:** `lightning_fast`
- **Условие:** Выполнил 20 задач за день
- **Триггер:** При выполнении задачи, когда tasks_completed_today >= 20

### 6. 💎 Безупречный (30 очков)
- **ID:** `flawless`
- **Условие:** Выполнил 50 задач без просрочки (в срок)
- **Триггер:** При выполнении задачи в срок, когда tasks_completed_on_time >= 50

### 7. 🏃 Марафонец (30 очков)
- **ID:** `marathon_runner`
- **Условие:** Выполнял задачи 30 дней подряд
- **Триггер:** При выполнении задачи, когда task_streak_current >= 30

### 8. 🏆 Мастер завершения (40 очков)
- **ID:** `completion_master`
- **Условие:** Выполнил 100 задач
- **Триггер:** При выполнении задачи, когда tasks_completed_total >= 100

---

## 🔧 Технические изменения

### Backend

#### 1. **models.py** - Новые поля в UserStats
```python
# Счетчики для достижений "Список дел"
tasks_created_total: int = 0  # Всего создано задач
tasks_completed_total: int = 0  # Всего выполнено задач
tasks_completed_today: int = 0  # Задач выполнено сегодня
tasks_completed_early: int = 0  # Задач выполнено до 9:00
tasks_completed_on_time: int = 0  # Задач выполнено в срок (без просрочки)
task_streak_days: int = 0  # Максимальная серия дней
task_streak_current: int = 0  # Текущая серия дней
last_task_completion_date: Optional[str] = None  # Дата последнего выполнения
last_daily_reset: Optional[str] = None  # Дата последнего сброса счетчика дня
first_task_created: bool = False  # Создана ли первая задача
```

#### 2. **achievements.py** - Новые достижения и action_type

**Добавлены action_type:**
- `create_task` - создание задачи
- `complete_task` - выполнение задачи (с metadata: hour, on_time)
- `reset_daily_tasks` - сброс дневных счетчиков (автоматически в 00:00)

**Логика отслеживания:**
- При создании задачи: увеличивается `tasks_created_total`, устанавливается `first_task_created = true`
- При выполнении задачи: 
  - Увеличивается `tasks_completed_total` и `tasks_completed_today`
  - Если час < 9: увеличивается `tasks_completed_early`
  - Если задача выполнена в срок (до дедлайна): увеличивается `tasks_completed_on_time`
  - Обновляется streak (серия дней):
    - Если это первое выполнение: streak = 1
    - Если следующий день подряд: streak += 1
    - Если пропущены дни: streak сбрасывается до 1

#### 3. **server.py** - Интеграция с API

**POST /api/tasks** (создание задачи):
```python
# После создания задачи
await achievements.track_user_action(
    db, 
    task_data.telegram_id, 
    "create_task",
    metadata={}
)
```

**PUT /api/tasks/{task_id}** (обновление задачи):
```python
# Если задача была выполнена (completed: false → true)
if is_completing:
    current_hour = datetime.utcnow().hour
    
    # Проверяем, выполнена ли в срок
    deadline = existing_task.get("deadline")
    on_time = True
    if deadline:
        on_time = datetime.utcnow() <= deadline
    
    # Отслеживаем выполнение
    await achievements.track_user_action(
        db,
        existing_task["telegram_id"],
        "complete_task",
        metadata={
            "hour": current_hour,
            "on_time": on_time
        }
    )
```

#### 4. **scheduler.py** - Сброс дневных счетчиков

**Новая задача планировщика:**
```python
# Сброс дневных счетчиков каждый день в 00:00 МСК
self.scheduler.add_job(
    self.reset_daily_task_counters,
    trigger=CronTrigger(hour=0, minute=0, timezone=MOSCOW_TZ),
    id='reset_daily_tasks',
    name='Reset daily task counters',
    replace_existing=True
)
```

**Метод сброса:**
```python
async def reset_daily_task_counters(self):
    # Сбрасывает tasks_completed_today для всех пользователей
    result = await self.db.user_stats.update_many(
        {},
        {"$set": {"tasks_completed_today": 0, ...}}
    )
```

---

## 🔄 Логика работы

### Создание задачи
1. Пользователь создает задачу через POST /api/tasks
2. Backend сохраняет задачу в MongoDB
3. Вызывается `track_user_action(db, telegram_id, "create_task")`
4. Увеличивается счетчик `tasks_created_total`
5. Проверяются достижения: "Первая задача" (1 задача) и "Специалист по задачам" (50 задач)

### Выполнение задачи
1. Пользователь отмечает задачу как выполненную через PUT /api/tasks/{task_id}
2. Backend проверяет, была ли задача не выполнена (completed: false → true)
3. Вызывается `track_user_action(db, telegram_id, "complete_task", metadata={hour, on_time})`
4. Обновляются счетчики:
   - `tasks_completed_total` (для "Мастер завершения" - 100 задач)
   - `tasks_completed_today` (для "Продуктивный день" - 5 задач, "Молния" - 20 задач)
   - `tasks_completed_early` если час < 9 (для "Рано встаешь" - 10 задач)
   - `tasks_completed_on_time` если выполнена до дедлайна (для "Безупречный" - 50 задач)
5. Обновляется streak:
   - Если это первое выполнение или новый день: обновляется `last_task_completion_date`
   - Если следующий день подряд: увеличивается `task_streak_current` (для "Марафонец" - 30 дней)
   - Если пропущены дни: `task_streak_current` сбрасывается до 1
6. Проверяются все достижения для задач

### Сброс дневных счетчиков
1. Каждый день в 00:00 МСК срабатывает планировщик
2. Для всех пользователей `tasks_completed_today` сбрасывается в 0
3. Обновляется `last_daily_reset` с текущей датой

---

## 🎯 Проверка достижений (в check_and_award_achievements)

```python
# Достижения для раздела "Список дел"
elif achievement_id == "first_task" and stats.first_task_created:
    earned = True
elif achievement_id == "productive_day" and stats.tasks_completed_today >= 5:
    earned = True
elif achievement_id == "early_riser_tasks" and stats.tasks_completed_early >= 10:
    earned = True
elif achievement_id == "task_specialist" and stats.tasks_created_total >= 50:
    earned = True
elif achievement_id == "lightning_fast" and stats.tasks_completed_today >= 20:
    earned = True
elif achievement_id == "flawless" and stats.tasks_completed_on_time >= 50:
    earned = True
elif achievement_id == "marathon_runner" and stats.task_streak_current >= 30:
    earned = True
elif achievement_id == "completion_master" and stats.tasks_completed_total >= 100:
    earned = True
```

---

## 📊 API Response при выполнении задачи

При выполнении задачи backend возвращает:
```json
{
  "new_achievements": [
    {
      "id": "first_task",
      "name": "Первая задача",
      "description": "Создал первую задачу",
      "emoji": "📝",
      "points": 5,
      "type": "first_task",
      "requirement": 1
    }
  ],
  "total_points_earned": 5
}
```

Frontend показывает всплывающее уведомление о новом достижении через компонент `AchievementNotification`.

---

## ✅ Статус реализации

- ✅ **Backend models.py** - добавлены поля для отслеживания задач
- ✅ **Backend achievements.py** - добавлены 8 достижений и логика проверки
- ✅ **Backend server.py** - интегрировано отслеживание в API создания и обновления задач
- ✅ **Backend scheduler.py** - добавлен автоматический сброс дневных счетчиков
- ✅ **Backend скомпилирован** - без ошибок
- ✅ **Backend запущен** - сервис работает

---

## 🧪 Тестирование

### Сценарий 1: Первая задача
1. Создать задачу через POST /api/tasks
2. Проверить, что вернулось достижение "Первая задача" (📝, 5 очков)

### Сценарий 2: Продуктивный день
1. Создать 5 задач
2. Выполнить 5 задач за один день
3. Проверить, что вернулось достижение "Продуктивный день" (✅, 15 очков)

### Сценарий 3: Рано встаешь
1. Выполнить 10 задач до 9:00 утра (hour < 9)
2. Проверить, что вернулось достижение "Рано встаешь" (🌅, 15 очков)

### Сценарий 4: Молния
1. Выполнить 20 задач за один день
2. Проверить, что вернулось достижение "Молния" (⚡, 25 очков)

### Сценарий 5: Безупречный
1. Выполнить 50 задач в срок (до дедлайна)
2. Проверить, что вернулось достижение "Безупречный" (💎, 30 очков)

### Сценарий 6: Марафонец
1. Выполнять хотя бы одну задачу каждый день в течение 30 дней
2. Проверить, что вернулось достижение "Марафонец" (🏃, 30 очков)

### Сценарий 7: Специалист по задачам
1. Создать 50 задач
2. Проверить, что вернулось достижение "Специалист по задачам" (🎯, 20 очков)

### Сценарий 8: Мастер завершения
1. Выполнить 100 задач
2. Проверить, что вернулось достижение "Мастер завершения" (🏆, 40 очков)

---

## 📝 Примечания

1. **Дневные счетчики** автоматически сбрасываются в 00:00 по московскому времени
2. **Streak (серия дней)** продолжается только если выполнять задачи каждый день подряд
3. **Задача считается "в срок"** если:
   - У нее нет дедлайна (on_time = true)
   - Выполнена до или в день дедлайна
4. **Выполнение до 9:00** определяется по часу выполнения (hour < 9)
5. **Все достижения проверяются автоматически** после каждого создания/выполнения задачи
6. **Обновлен счетчик "Перфекционист"** - теперь требует 23 достижения (было 15)

---

## 🎉 Итого

Добавлено **8 новых достижений** для раздела "Список дел" на **200 очков** суммарно:
- Базовые: 5 + 15 + 15 + 20 = **55 очков** (4 достижения)
- Продвинутые: 25 + 30 + 30 + 40 = **125 очков** (4 достижения)

Система мотивирует пользователей:
- ✅ Создавать задачи
- ✅ Выполнять их регулярно
- ✅ Быть продуктивными
- ✅ Соблюдать сроки
- ✅ Вставать рано
- ✅ Поддерживать серию дней

**Геймификация раздела "Список дел" полностью реализована!** 🚀
