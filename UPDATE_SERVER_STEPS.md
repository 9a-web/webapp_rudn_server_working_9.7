# 🎯 Пошаговая инструкция: Обновление сервера с сохранением данных

## Ваша ситуация:
- ✅ Есть локальный репозиторий с обновлениями
- ✅ Есть серверная версия с рабочей БД (не пустой)
- 🎯 Нужно: обновить сервер, не потеряв данные

---

## 🚀 Простой процесс (3 основных шага)

### 📦 ШАГ 1: Создать бэкап (на сервере)

**Подключитесь к серверу и выполните:**

```bash
cd /app
./download_backup.sh
```

**Сохраните бэкап локально:**

```bash
# Закодировать файл
cat /app/api_backups/database_backup_*.json | base64 > /tmp/backup.txt

# Показать (скопировать весь вывод)
cat /tmp/backup.txt
```

**На вашем локальном компьютере:**
```bash
# Создать файл с содержимым (вставить скопированный текст)
nano backup_encoded.txt

# Декодировать
cat backup_encoded.txt | base64 -d > server_backup_$(date +%Y%m%d).json

# Убедиться что файл создан
ls -lh server_backup_*.json
```

✅ **Готово! Теперь у вас есть копия базы данных.**

---

### 🔄 ШАГ 2: Обновить код (на сервере)

**Если используете Git:**

```bash
cd /app

# Проверить текущее состояние
git status

# Обновить код
git pull origin main
```

**Если копируете файлы вручную:**

Просто скопируйте измененные файлы из локального репозитория на сервер.
**НЕ КОПИРУЙТЕ:**
- `.env` файлы
- Директории с данными MongoDB
- `node_modules` или `__pycache__`

**КОПИРУЙТЕ:**
- Измененные `.py` файлы (backend)
- Измененные `.js/.jsx` файлы (frontend)
- `requirements.txt` (если изменился)
- `package.json` (если изменился)

---

### 🔧 ШАГ 3: Перезапустить приложение (на сервере)

**Если обновились зависимости:**

```bash
# Backend (если изменился requirements.txt)
cd /app/backend
pip install -r requirements.txt

# Frontend (если изменился package.json)
cd /app/frontend
yarn install
```

**Перезапустить сервисы:**

```bash
sudo supervisorctl restart all
```

**Проверить что всё работает:**

```bash
# Статус сервисов
sudo supervisorctl status

# Проверить данные
curl http://localhost:8001/api/backup/stats | python3 -m json.tool
```

---

## ✅ Готово!

Ваш сервер обновлён, данные на месте!

---

## 🔍 Проверка что всё в порядке

```bash
# Количество документов в БД
mongosh mongodb://localhost:27017/rudn_schedule --eval "
  print('👥 Пользователей: ' + db.user_settings.countDocuments());
  print('📝 Задач: ' + db.tasks.countDocuments());
  print('🏠 Комнат: ' + db.rooms.countDocuments());
"

# Проверить логи
tail -n 50 /var/log/supervisor/backend.out.log
tail -n 50 /var/log/supervisor/frontend.out.log
```

---

## ❓ Почему данные не удаляются?

MongoDB хранит данные **отдельно от кода**:

```
Ваш проект:
├── /app/backend/         ← Код backend (обновляется)
├── /app/frontend/        ← Код frontend (обновляется)
└── /var/lib/mongodb/     ← База данных (НЕ трогается!)
```

При обновлении кода через Git или копирование файлов:
- ✅ Обновляются файлы `.py`, `.js`, `.jsx`
- ✅ Обновляются зависимости
- ❌ База данных **НЕ изменяется**

Данные удаляются только если:
- Вы явно выполните команду удаления (`db.dropDatabase()`)
- Удалите директорию `/var/lib/mongodb/`
- Выполните команду `/clear_db` в боте

---

## 🆘 Что делать если что-то пошло не так?

### Проблема: Сервисы не запускаются

```bash
# Проверить логи ошибок
tail -n 100 /var/log/supervisor/backend.err.log
tail -n 100 /var/log/supervisor/frontend.err.log

# Возможно не установлены зависимости
cd /app/backend && pip install -r requirements.txt
cd /app/frontend && yarn install

# Перезапустить
sudo supervisorctl restart all
```

### Проблема: Данные пропали (маловероятно)

```bash
# Проверить подключение к БД
mongosh mongodb://localhost:27017/rudn_schedule --eval "db.stats()"

# Если БД на месте - проверить .env
cat /app/backend/.env | grep MONGO_URL

# Восстановить из бэкапа (если нужно)
./restore_mongodb.sh /path/to/backup
```

### Проблема: Откатиться на предыдущую версию

```bash
# Посмотреть историю
git log --oneline -10

# Откатиться
git checkout PREVIOUS_COMMIT_HASH

# Перезапустить
sudo supervisorctl restart all
```

---

## 💡 Полезные команды

### Сравнить локальную и серверную версию

```bash
# На сервере - посмотреть последний коммит
git log -1

# На локальном ПК - посмотреть последний коммит
git log -1

# Сравнить
```

### Посмотреть что изменилось

```bash
git diff HEAD~1  # Сравнить с предыдущим коммитом
git log -p -2    # Показать изменения в последних 2 коммитах
```

---

## 📚 Дополнительные материалы

- **Полное руководство:** [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)
- **Про бэкапы:** [BACKUP_GUIDE.md](./BACKUP_GUIDE.md)
- **Шпаргалка:** [BACKUP_CHEATSHEET.md](./BACKUP_CHEATSHEET.md)

---

## 🎉 Резюме

1. **Бэкап** → Скачать на локальный ПК
2. **Обновить** → Git pull или копирование файлов
3. **Перезапустить** → supervisorctl restart all
4. **Проверить** → Логи и данные

**Данные в MongoDB не удаляются при обновлении кода!**

Но всегда делайте бэкап - это страховка на всякий случай! 🛡️
