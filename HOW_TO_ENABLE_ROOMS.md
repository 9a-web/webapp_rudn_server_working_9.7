# 🔧 Как включить функцию комнат

> **Статус:** Функция комнат СКРЫТА, но полностью рабочая. Можно включить в любой момент.

## Быстрое включение (5 минут)

### Вариант 1: Автоматически (одна команда)

```bash
sed -i 's/const SHOW_ROOMS_FEATURE = false;/const SHOW_ROOMS_FEATURE = true;/g' /app/frontend/src/components/TasksSection.jsx && sudo supervisorctl restart frontend
```

✅ Готово! Откройте приложение и перейдите в "Список дел" - секция комнат появится внизу.

---

### Вариант 2: Вручную (рекомендуется для контроля)

#### 1. Откройте файл

```bash
nano /app/frontend/src/components/TasksSection.jsx
```

#### 2. Найдите строку 21 и измените:

```javascript
// БЫЛО:
const SHOW_ROOMS_FEATURE = false;

// ИЗМЕНИТЬ НА:
const SHOW_ROOMS_FEATURE = true;
```

Сохраните: `Ctrl+O`, Enter, `Ctrl+X`

#### 3. Перезапустите frontend

```bash
sudo supervisorctl restart frontend
```

#### 4. Проверьте результат

Откройте приложение → "Список дел" → Прокрутите вниз

✅ Должна появиться секция **"Комнаты"** с кнопкой "Создать комнату"

---

## Проверка работоспособности

### ✅ Что должно работать:

1. **Создание комнаты:**
   - Нажмите "Создать комнату"
   - Заполните название и описание
   - Выберите цвет
   - Комната создается и появляется в списке

2. **Просмотр комнаты:**
   - Нажмите на карточку комнаты
   - Открывается модальное окно с деталями
   - Видны участники и задачи

3. **Генерация ссылки-приглашения:**
   - В деталях комнаты нажмите "Пригласить участников"
   - Генерируется уникальная ссылка для Telegram бота

4. **Создание командных задач:**
   - В комнате нажмите "Добавить задачу"
   - Создайте задачу - она доступна всем участникам комнаты

---

## Возможные проблемы

### ❌ Секция комнат не появилась

**Причина 1:** Изменения не применились
```bash
# Проверьте содержимое файла
grep "SHOW_ROOMS_FEATURE" /app/frontend/src/components/TasksSection.jsx
# Должно показать: const SHOW_ROOMS_FEATURE = true;
```

**Причина 2:** Frontend не перезапустился
```bash
sudo supervisorctl status frontend
# Должно быть: frontend RUNNING
```

**Причина 3:** Кеш браузера
- Очистите кеш: `Ctrl+Shift+R` (Windows/Linux) или `Cmd+Shift+R` (Mac)

---

### ❌ Ошибки в консоли браузера

```bash
# Проверьте логи
tail -n 100 /var/log/supervisor/frontend.err.log
```

Если видите ошибки импорта (RoomCard, CreateRoomModal и т.д.), проверьте наличие файлов:
```bash
ls -la /app/frontend/src/components/Room*.jsx
ls -la /app/frontend/src/services/roomsAPI.js
```

Все файлы должны существовать!

---

### ❌ Backend API не отвечает

```bash
# Проверьте backend
sudo supervisorctl status backend

# Тестовый запрос
curl http://localhost:8001/api/rooms/123456789
```

Если ошибка, перезапустите:
```bash
sudo supervisorctl restart backend
```

---

## Быстрое отключение (если нужно)

```bash
sed -i 's/const SHOW_ROOMS_FEATURE = true;/const SHOW_ROOMS_FEATURE = false;/g' /app/frontend/src/components/TasksSection.jsx && sudo supervisorctl restart frontend
```

---

## Техническая информация

### Затронутые файлы:
- **Frontend:** `/app/frontend/src/components/TasksSection.jsx` (строка 21)
- **Компоненты комнат:** RoomCard.jsx, CreateRoomModal.jsx, RoomDetailModal.jsx
- **API сервис:** `/app/frontend/src/services/roomsAPI.js`

### Backend endpoints (все рабочие):
- `POST /api/rooms` - Создание комнаты
- `GET /api/rooms/{telegram_id}` - Список комнат пользователя
- `GET /api/rooms/detail/{room_id}` - Детали комнаты
- `POST /api/rooms/{room_id}/invite-link` - Генерация ссылки-приглашения
- `POST /api/rooms/join/{invite_token}` - Присоединение к комнате
- `POST /api/rooms/{room_id}/tasks` - Создание задачи в комнате
- `GET /api/rooms/{room_id}/tasks` - Получение задач комнаты
- `DELETE /api/rooms/{room_id}/leave` - Выход из комнаты
- `DELETE /api/rooms/{room_id}` - Удаление комнаты (только владелец)

### База данных:
Все данные комнат сохранены в MongoDB в коллекциях:
- `rooms` - информация о комнатах
- `room_participants` - участники комнат
- `group_tasks` - задачи комнат

---

## Контакты для помощи

Если возникли проблемы при включении функции комнат:

1. Проверьте полную документацию: `/app/ROOMS_FEATURE_HIDDEN.md`
2. Посмотрите логи: `tail -n 100 /var/log/supervisor/*.log`
3. Обратитесь к разработчику с копией логов

---

**Последнее обновление:** 16 ноября 2024  
**Версия документа:** 1.0
