# Автоматическое перемещение выполненных задач вниз списка

## Проблема
Выполненные задачи оставались на своих местах в списке, перемешиваясь с активными задачами. Это затрудняло фокусировку на текущих делах.

## Решение
Изменена логика сортировки задач в компоненте `TasksSection.jsx`, чтобы выполненные задачи автоматически перемещались вниз списка.

## Изменения

**Файл**: `/app/frontend/src/components/TasksSection.jsx`

### Новая логика сортировки (многоуровневая):

```javascript
allTasks.sort((a, b) => {
  // 1. СНАЧАЛА ПО СТАТУСУ ВЫПОЛНЕНИЯ (невыполненные сверху)
  if (a.completed !== b.completed) {
    return a.completed ? 1 : -1; // false (невыполненные) идут первыми
  }
  
  // 2. Потом по order (если установлен) - только для задач с одинаковым статусом
  const orderA = a.order !== undefined ? a.order : 999999;
  const orderB = b.order !== undefined ? b.order : 999999;
  
  if (orderA !== orderB) {
    return orderA - orderB;
  }
  
  // 3. Потом по приоритету
  const priorityA = priorityOrder[a.priority] || 2;
  const priorityB = priorityOrder[b.priority] || 2;
  return priorityB - priorityA;
});
```

## Порядок сортировки

### Приоритеты сортировки:
1. **Статус выполнения** (высший приоритет)
   - ❌ Невыполненные задачи → сверху
   - ✅ Выполненные задачи → вниз

2. **Пользовательский порядок** (order)
   - Сохраняется порядок drag & drop для задач с одинаковым статусом

3. **Приоритет задачи** (низший приоритет)
   - 🔴 Высокий (high) → сверху
   - 🟡 Средний (medium) → в середине
   - 🟢 Низкий (low) → ниже

## Примеры работы

### До изменения:
```
1. ✅ Купить молоко [high]
2. ❌ Подготовиться к экзамену [high]
3. ✅ Позвонить маме [medium]
4. ❌ Сделать уборку [low]
```

### После изменения:
```
1. ❌ Подготовиться к экзамену [high]
2. ❌ Сделать уборку [low]
3. ✅ Купить молоко [high]
4. ✅ Позвонить маме [medium]
```

## Поведение при отметке задачи

**Сценарий**: Пользователь отмечает задачу "Подготовиться к экзамену" как выполненную

**Действия**:
1. Пользователь кликает на checkbox задачи
2. Задача отмечается как `completed: true`
3. Backend обновляет статус задачи
4. Frontend пересортировывает массив задач
5. **Задача автоматически перемещается вниз списка** ⬇️
6. Плавная анимация перемещения (благодаря Framer Motion Reorder)

## UX преимущества

✅ **Фокус на активных задачах**: Невыполненные задачи всегда на виду
✅ **Визуальное разделение**: Четкая граница между активными и завершенными
✅ **Мотивация**: Видно, как список активных задач сокращается
✅ **Drag & Drop сохраняется**: Можно менять порядок внутри каждой группы

## Технические детали

### Функция сортировки
- Использует тернарный оператор: `a.completed ? 1 : -1`
- `false` (невыполненные) возвращают `-1` → идут выше
- `true` (выполненные) возвращают `1` → идут ниже

### Интеграция с Reorder
```javascript
<Reorder.Group 
  values={todayTasks} 
  onReorder={handleReorder}
>
  {todayTasks.map(task => (
    <TodayTaskItem key={task.id} task={task} />
  ))}
</Reorder.Group>
```

Framer Motion автоматически анимирует перемещение задач при изменении порядка.

### Сохранение на сервере
Когда пользователь вручную перетаскивает задачи (drag & drop), порядок сохраняется в поле `order`. Сортировка уважает этот порядок **внутри групп** (невыполненные / выполненные).

## Альтернативные подходы (не реализованы)

❌ **Полное разделение списков**: Создание двух отдельных секций "Активные" и "Завершенные"
- Минус: Занимает больше места, требует прокрутки

❌ **Скрытие выполненных**: Полное удаление выполненных задач из вида
- Минус: Пользователь теряет контекст сделанной работы

✅ **Текущий подход**: Единый список с автоматической сортировкой
- Плюс: Компактно, наглядно, сохраняет контекст

## Файлы изменены
- `/app/frontend/src/components/TasksSection.jsx` (строки 436-454)

## Дата изменения
2025-01-XX

## Статус
✅ Реализовано и готово к тестированию
